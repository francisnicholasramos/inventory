<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= item.item_name %> | Velocité</title>
    <link href="/styles/build.css" rel="stylesheet" />
  </head>
  <body>
    <%- include('partials/header') %>

    <div class="flex mx-auto mt-10 h-full w-full min-w-[320px] max-w-[1200px] gap-10">
      <div class="w-1/2 max-h-96 border border-gray-300 rounded-lg overflow-hidden shadow">
          <img
            src="<%= item.image %>"
            alt="<%= item.item_name %>"
            loading="lazy"
            class="w-full h-full object-cover object-center"
          />
      </div>

      <div class="w-1/2 flex flex-col gap-4">
        <h1 class="text-3xl font-bold text-[#163933]"><%= item.item_name %></h1>
        <p class="text-gray-700 text-lg"><%= item.description %></p>
        <p class="text-2xl font-bold text-[#1d4942]">₱<%= item.price %></p>

        <a
          href="/components"
          class="w-fit mt-4 bg-[#1d4942] text-[#fff8da] px-4 py-2 rounded hover:bg-[#163933] transition"
        >
          ← Back to items
        </a>
      </div>
    </div>
  </body>
</html>
